// selected elements
const goodCheck = document.getElementsByClassName("good-Check")[0];
const cheapCheck = document.getElementsByClassName("cheap-Check")[0];
const fastCheck = document.getElementsByClassName("fast-Check")[0];

// gelobal varibales

// functions

//event linsters
goodCheck.addEventListener("click", goodHandler);
cheapCheck.addEventListener("click", cheapHandler);
fastCheck.addEventListener("click", fastHandler);
